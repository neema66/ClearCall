#!/usr/bin/env python
"""
Placeholder script for downloading/preparing sample evaluation data
(the DNS Challenge dataset or a small subset of it).

TODO (Member 4 / Evaluation lead): implement this to either:
  (a) download a small official DNS Challenge subset, or
  (b) synthesize clean/noisy pairs locally (e.g. clean speech + mixed-in
      noise clips at a few SNR levels) if the full dataset is too large
      to download for weekly iteration.

For now, see data/README.md for how to manually place a few sample
clean/noisy WAV pairs to get started before this script is implemented.
"""

import sys

print(
    "TODO: this script is a placeholder.\n"
    "See data/README.md for how to manually add a few sample clean/noisy "
    "WAV pairs to data/clean/ and data/noisy/ in the meantime."
)
sys.exit(0)
